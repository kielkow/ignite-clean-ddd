describe('DomainEvents', () => {
	it('should add aggregate to markedAggregates', () => {})

	it('should dispatch aggregate events', () => {})

	it('should remove aggregate from markedAggregates', () => {})

	it('should return aggregate if found', () => {})

	it('should return undefined if not found', () => {})

	it('should dispatch events for aggregate', () => {})
})
